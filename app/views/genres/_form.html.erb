</br>
</br>
</br>
<%= form_with model: @genre, class: 'form-horizontal' do |f| %>
	<%= render 'errors', object: @genre %>
	<div class="form-group">
	  <%= f.label 'Gênero', class: 'col-sm-2 control-label' %>
	  <div class="col-sm-10">
		<%= f.text_field :name, class: 'form-control' %>
		<%= @genre.errors[:name].join(", ") if @genre.errors[:name].any? %>
	  </div>
	</div>

	<%= f.submit 'Salvar', class: 'btn btn-secondary' %>
  <% end %>